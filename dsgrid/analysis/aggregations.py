
from pyspark.sql import SparkSession


def aggregate_many_to_one(data_table, lookup, mapping):
    """Return a dataframe with results aggregated by the mapping.

    Parameters
    ----------
    data_table : str
    mapping : ManyToOneMapping

    """
    pass
